#!/bin/bash

# Python-Skript ausführen
python3 script.py stage

# Überprüfen, ob das Skript erfolgreich war
if [ $? -ne 0 ]; then
    echo "Python script failed. Commit aborted."
    exit 1
fi

# Änderungen staggen
git add .

echo "Changes were staged."
